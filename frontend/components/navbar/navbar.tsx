"use client";

const Navbar = () => {
  const navbarClasses = `sticky top-0 z-50 w-full bg-white/70 backdrop-blur-md shadow-md dark:bg-gray-900/70 transition-colors duration-300`;

  return (
    <nav className={navbarClasses}>
      <div className="flex items-center justify-center">
        {/* logo */}
        {/* menu */}
      </div>
      {/* action button */}
    </nav>
  );
};

export default Navbar;
